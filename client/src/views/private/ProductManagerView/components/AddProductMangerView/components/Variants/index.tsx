import { Box, Divider, Heading } from "@chakra-ui/layout";
import React from "react";

type Props = {};

const Variants = (props: Props) => {
	return <Box>Variants</Box>;
};

export default Variants;
